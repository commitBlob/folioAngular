// Jest setup — replaces the Karma-specific src/test.ts.
// Bootstraps the Angular testing environment, zone patches and browser mocks.
import 'jest-preset-angular';
import './setup-jest-global-mocks';
